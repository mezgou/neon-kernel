# neon-kernel
Processing PPM images using CUDA

## Build and Run project

```
scripts/build.py --build-type release|debug
scripts/run.py --build-type release|debug -i assets/test.ppm -e blur -v 0.5
```

